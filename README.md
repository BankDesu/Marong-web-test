How to Install
pip install -r requirement.txt

How to Run Tests
python -m robot --outputdir (folder-test)/results (folder-test)/(filename).robot
example: python -m robot --outputdir login-test/results login-test/logintest.robot